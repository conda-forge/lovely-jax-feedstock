{% set name = "lovely-jax" %}
{% set version = "0.1.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/lovely-jax-{{ version }}.tar.gz
  sha256: e4c94ace6d8a52f0ed63bb05f07f2950a03da92b170c74d3190f8f46f829c67c

build:
  noarch: python
  number: 0
  # The script can have multiple lines. Hence, keep it at the end.
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - python >=3.7
    - pip
  run:
    - python >=3.7
    - jax
    - lovely-numpy >=0.2.8

test:
  imports:
    - lovely_jax
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/xl0/lovely-jax
  summary: 💘 Lovely JAX
  license: MIT
  license_file: LICENSE
  description: |

    PyPI: [https://pypi.org/project/lovely-jax/](https://pypi.org/project/lovely-jax/)

  dev_url: https://github.com/xl0/lovely-jax
  doc_url: https://xl0.github.io/lovely-jax/

extra:
  recipe-maintainers:
    - sugatoray
    - xl0
